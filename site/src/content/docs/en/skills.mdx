---
title: Skills Index
description: Every ASDT specialist at a glance — what each one does, the command that invokes it, and where it sits in the pipeline.
order: 19
locale: en
---

import SkillsIndex from '@components/SkillsIndex.astro'
import { useTranslations } from '@i18n/utils'

export const t = useTranslations('en')

# Skills Index

Every ASDT specialist at a glance, in pipeline order. Each one is independently invocable by its command — use this grid to pick the right specialist when you only need one perspective.

<SkillsIndex t={t} locale="en" />

When the work spans multiple perspectives, skip the grid and run `/asdt <feature description>` — the orchestrator classifies the change and routes it through the right specialists in the right order.
